package com.booking.spring.login.repository;

import com.booking.spring.login.models.DepartmentImages;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DepartmentImagesRepository extends JpaRepository<DepartmentImages, Long> {
}
